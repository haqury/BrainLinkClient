# 🤖 Автоматическое обучение ML модели

## 📋 Обзор

BrainLink Client теперь поддерживает **автоматическое обучение ML модели** во время игры! Модель будет автоматически переобучаться, когда собирается достаточно новых данных.

---

## ✨ Как это работает

### Процесс автоматического обучения:

1. **Игра отправляет события** через Shared Memory (Type 2 команды)
2. **BrainLink сохраняет** EEG данные + событие как training sample
3. **После N новых образцов** (по умолчанию 5) система проверяет:
   - Достаточно ли данных для обучения (минимум 10 на класс)
   - Если да → запускает обучение в фоне
4. **Модель переобучается** автоматически
5. **Уведомление** пользователю о завершении

---

## ⚙️ Настройки

### В UI (Main Window)

В разделе **"Game Integration (Shared Memory)"** есть чекбоксы:

- ✅ **"Accept commands from games"** - принимать команды от игр
- ✅ **"Auto-save to history"** - сохранять события в историю
- ✅ **"Auto-save for ML training"** - сохранять для ML обучения
- ✅ **"Auto-train model"** - автоматически переобучать модель

### В конфигурации ML (`MLConfig`)

```python
auto_train_enabled: bool = True  # Включить автообучение
auto_train_min_new_samples: int = 5  # Минимум новых образцов перед обучением
min_samples_per_class: int = 10  # Минимум образцов на класс для обучения
```

---

## 🎮 Использование в игре

### Пример кода для игры:

```python
from examples.shared_memory_client import BrainLinkSharedMemoryClient

client = BrainLinkSharedMemoryClient()
client.connect()

# В игровом цикле, когда происходит событие:
if player_jumped:
    # Отправить событие для ML обучения
    client.send_event_for_ml_training("ml")  # или "mr", "mu", "md", "action"
```

### Что происходит:

1. Игра отправляет событие `"ml"` (move left)
2. BrainLink получает текущие EEG данные
3. Сохраняет как training sample: `{EEG данные, event: "ml"}`
4. После 5 новых образцов → автоматически переобучает модель
5. Модель становится точнее!

---

## 📊 Логирование

### События в логах:

```
[INFO] Added ML training sample 'ml' from game
[INFO] Starting auto-training (new samples: 5)
[INFO] Background training started
[INFO] Training random_forest model...
[INFO] Training accuracy: 0.850
[INFO] Testing accuracy: 0.820
[INFO] Background training completed: accuracy=0.820, samples=45
[INFO] 🔄 ML model was updated
```

### Уведомления в System Tray:

- 🟢 **"ML Training"** - Обучение началось
- ✅ **"ML Training Complete"** - Обучение завершено с метриками
- ⚠️ **"ML Training Failed"** - Ошибка обучения

---

## 🔧 Технические детали

### Thread Safety

- Обучение происходит в **отдельном потоке** (QThread)
- Не блокирует UI
- Использует `Lock` для thread-safety

### Производительность

- Обучение запускается только при:
  - Достаточно новых образцов (≥ 5)
  - Достаточно данных на класс (≥ 10)
- Если обучение уже идёт, новые запросы игнорируются
- Модель сохраняется автоматически после обучения

### Ограничения

- Минимум **10 образцов на класс** для обучения
- Минимум **5 новых образцов** для запуска автообучения
- Обучение может занять несколько секунд (зависит от размера данных)

---

## 🎯 Best Practices

### Для игр:

1. **Отправляйте события часто** - чем больше данных, тем лучше модель
2. **Отправляйте реальные события** - только когда действие действительно произошло
3. **Балансируйте классы** - старайтесь отправлять все типы событий (ml, mr, mu, md, action)

### Для пользователей:

1. **Включите "Auto-train model"** для автоматического улучшения
2. **Собирайте данные постепенно** - не нужно собирать всё сразу
3. **Проверяйте уведомления** - они показывают прогресс обучения

---

## 🐛 Troubleshooting

### Модель не обучается автоматически

**Проблема:** Автообучение не запускается

**Решения:**
1. Проверьте, что включен чекбокс "Auto-train model"
2. Убедитесь, что есть минимум 10 образцов для каждого класса
3. Проверьте логи на ошибки

### Ошибка "Not enough samples"

**Проблема:** Недостаточно данных для обучения

**Решение:** Соберите больше training samples. Минимум 10 на каждый класс событий.

### Обучение занимает слишком долго

**Проблема:** Обучение блокирует приложение

**Решение:** Обучение уже происходит в фоне и не должно блокировать UI. Если блокирует - проверьте логи на ошибки.

---

## 📈 Метрики

После каждого обучения вы увидите:

- **Training accuracy** - точность на обучающей выборке
- **Testing accuracy** - точность на тестовой выборке
- **Number of samples** - общее количество образцов
- **Classification report** - детальная статистика по классам

---

## 🔮 Будущие улучшения

- [ ] Incremental learning (обучение без полного переобучения)
- [ ] Cloud sync для общей модели
- [ ] A/B testing разных моделей
- [ ] Автоматическая оптимизация гиперпараметров
- [ ] Визуализация прогресса обучения

---

**Версия:** 1.0  
**Дата:** 20.01.2026  
**Статус:** ✅ Реализовано
