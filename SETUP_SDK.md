# Настройка BrainLink SDK для работы с реальным устройством

## Вариант 1: Использование BrainLinkSDK_Windows.dll (Рекомендуется)

### Шаг 1: Установите pythonnet

```bash
pip install pythonnet
```

### Шаг 2: Скопируйте DLL файлы

Скопируйте следующие файлы из C# проекта в папку `services/`:

```
C:\Users\haqury\source\repos\BrainLinkConnect\bin\Release\
├── BrainLinkSDK_Windows.dll   ← Основная DLL
├── wclBluetoothFramework.dll  ← Зависимость
├── wclCommon.dll              ← Зависимость
└── wclCommunication.dll       ← Зависимость
```

Скопируйте в:
```
C:\Users\haqury\PycharmProjects\BrainLinkClient\services\
```

### Шаг 3: Запустите приложение

```bash
python main.py
```

При запуске вы увидите:
- ✅ "BrainLink SDK loaded successfully" - если DLL найдена
- ⚠️ "DLL not found" - если DLL не найдена

### Шаг 4: Подключитесь к устройству

1. Нажмите "Start & Connect"
2. Выберите ваше BrainLink устройство
3. Нажмите "Connect"
4. Данные должны начать поступать!

---

## Вариант 2: Ручная интеграция протокола (Сложнее)

Если DLL не работает, нужно реализовать парсинг протокола вручную.

### Что нужно узнать:

1. **UUID характеристик Bluetooth**
   - UUID для EEG данных
   - UUID для гироскопа
   - UUID для расширенных данных

2. **Формат пакетов данных**
   - Заголовок пакета
   - Расположение данных (какие байты за что отвечают)
   - Контрольная сумма

### Как это сделать:

#### Запустите отладку:

```bash
python main.py
```

Подключитесь к устройству. В консоли будет выводиться:

```
📡 Received from Characteristic...
   HEX: aa5500410032001e00...
   Length: 32 bytes
   Raw bytes: [170, 85, 0, 65, 0, 50, ...]
```

#### Скопируйте эти данные и отправьте мне

Я помогу реализовать парсинг!

---

## Проверка установки pythonnet

```python
# Запустите в Python
import clr
print("✅ pythonnet установлен!")
```

Если ошибка:
```bash
pip install pythonnet
```

---

## Структура после установки

```
BrainLinkClient/
├── services/
│   ├── brainlink_sdk_wrapper.py      ← Обёртка для DLL
│   ├── BrainLinkSDK_Windows.dll      ← Скопированная DLL
│   ├── wclBluetoothFramework.dll
│   ├── wclCommon.dll
│   └── wclCommunication.dll
└── main.py
```

---

## Проверка работы SDK

Запустите:

```bash
python main.py
```

Смотрите вывод:

### Успешная загрузка:
```
Found DLL at: C:\Users\haqury\source\repos\BrainLinkConnect\bin\Release\BrainLinkSDK_Windows.dll
✅ BrainLink SDK loaded successfully
✅ Event handlers attached
```

### DLL не найдена:
```
⚠️ BrainLinkSDK_Windows.dll not found in default locations
Please provide path to DLL or copy it to project directory
```

---

## Быстрая команда для копирования DLL (PowerShell)

```powershell
Copy-Item "C:\Users\haqury\source\repos\BrainLinkConnect\bin\Release\*.dll" `
          -Destination "C:\Users\haqury\PycharmProjects\BrainLinkClient\services\" `
          -Include "BrainLinkSDK_Windows.dll","wcl*.dll"
```

---

## Если pythonnet не работает

Альтернатива - использовать симулятор для тестирования:

1. Нажмите "Start & Connect"
2. Нажмите "🎮 Use Simulator (for testing)"
3. Все функции будут работать с тестовыми данными

---

## Помощь

Если возникли проблемы:

1. **Проверьте версию Python**: должен быть Python 3.8+
2. **Проверьте pythonnet**: `pip install pythonnet`
3. **Проверьте наличие DLL файлов**
4. **Скопируйте вывод консоли** и отправьте мне

---

**После настройки всё должно работать как в оригинальной C# версии!** 🎉
